<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/violet_dark</item>
        <!-- inactive thumb color -->
        <!--        <item name="colorSwitchThumbNormal">@color/teal_700</item>-->
        <!-- inactive track color (30% transparency) -->
    </style>

    <style name="tabLayout" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="trackColorActive">@color/purple_700</item>
        <item name="android:colorForeground">@color/purple_700</item>
    </style>

    <style name="CustomTextStyle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/ts18</item>
    </style>
    <style name="LoadingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowMinWidthMajor">0%</item>
        <item name="android:windowMinWidthMinor">0%</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>